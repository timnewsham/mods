
const int CYCLE = 0x200;

// these don't all need dutytheta, but we want to use the
// same call signature for all of these functions
int intsin(int theta, int dutytheta);
int intsquare(int theta, int dutytheta);
int intsaw(int theta, int dutytheta);
int inttri(int theta, int dutytheta);

