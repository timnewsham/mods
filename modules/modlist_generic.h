// modlist_generic.cpp
//	A listing of all the system-independant modules

    // This is just a list of all the module descriptions that are
    // define in the module files.  Put your module here when you want 
    // to add it to the program.  (MOD() is just a macro defined before
    // this file is included to allow us to do some magic tricks with
    // the list).
    MOD(descr_examplemodule),

    MOD(descr_absmodule),
    MOD(descr_capmodule),
    MOD(descr_constmodule),
    MOD(descr_delaymodule),
    MOD(descr_envmodule),
    MOD(descr_filtmodule),	// XXX not done yet
    MOD(descr_freqmodule),
    MOD(descr_genmodule),
    MOD(descr_lfomodule),
    MOD(descr_mixmodule),
    MOD(descr_modulatemodule),
    MOD(descr_multmodule),
    MOD(descr_sampmodule),
    MOD(descr_splitmodule),
    MOD(descr_summodule),
    MOD(descr_switchmodule),
    MOD(descr_vocodemodule)

